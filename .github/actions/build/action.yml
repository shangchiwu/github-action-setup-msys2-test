name: Build
description: Build binaries
runs:
  using: composite
  steps:
    - name: Add MSYS toolchain to path
      run: echo C:\\msys64\\mingw64\\bin >> $GITHUB_PATH
      shell: pwsh

    - name: Run build.bat
      run: ./build.bat
      shell: pwsh
